# Known Issues

## AI-agnostic Collaboration Mode - Non-Critical Bugs

### 1. Phase 4: `req_node_id` undefined error
**Status**: Non-critical (does not affect code generation)
**Location**: `devpal/core/openspec_phases/phase4_generate_code.py`
**Description**: After generating code files, Phase 4 attempts to update artifact graph but fails with `cannot access local variable 'req_node_id' where it is not associated with a value`
**Impact**: Code is generated successfully, but artifact graph update fails
**Priority**: P2 (fix in next iteration)

### 2. Phase 9.5: Critique phase LLM client error
**Status**: Non-critical (Quality Gate still passes)
**Location**: `devpal/core/openspec_phases/phase9_code_review.py`
**Description**: `'LLMClient' object has no attribute 'create_message'`
**Impact**: LLM-as-a-Judge critique fails, but main Quality Gate validation still works
**Priority**: P2 (fix in next iteration)

## Fixed Issues (2026-06-04)

### 1. ✅ Phase skip logic indentation bug
**Fixed**: All phases were being skipped in run modes due to incorrect indentation (lines 634-643)
**Solution**: Indented skip_data block inside the `if not self.mode_policy.should_run_phase(i)` block

### 2. ✅ Rule Pack generation: change_id not available
**Fixed**: In PROPOSE_ONLY mode, `self.change_id` was None because it's generated in Phase 1
**Solution**: Read `change_id` from `context.current_change_id` (generated by Phase 1)

### 3. ✅ Rule Pack files generated in wrong directory
**Fixed**: Rule Pack files were generated in `requirements/` instead of project directory
**Solution**: Changed `project_root` from `self.context.requirements_file.parent` to `self.context.project_dir`
